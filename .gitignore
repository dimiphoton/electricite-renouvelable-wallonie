__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.pytest_cache/
.DS_Store

# Données brutes : pas de commit (taille/licence), on garde juste la structure
data/raw/*
!data/raw/.gitkeep

# Décommente si les modèles deviennent trop volumineux pour git classique
# models/*.pkl
# models/*.pt
